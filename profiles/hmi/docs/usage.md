# HMI Profile Usage

1. Generate plan with `test-plan-architect`
2. Run `industrial-test-orchestrator`
3. Use `hmi-e2e-verifier` for regression and evidence design
4. Use `security-62443-assessor` only for security-relevant UI / auth / exposure checks

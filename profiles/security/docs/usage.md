# Security Profile Usage

1. Generate plan with `test-plan-architect`
2. Use `security-62443-assessor` for mapping and gap analysis
3. Use `vuln-scan-triage` for scan normalization and reachability triage
4. Never call real bench tools from this profile

# Security Example Plan

- Scope: threat model, attack surface, account/auth, session, logging, update/integrity, scan triage
- Evidence: docs, configs, scan bundles, interface list
- Output: requirement/evidence mapping + gaps + residual risk + retest plan
